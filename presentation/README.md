# Final Recorded Presentation

The final recorded presentation — a single MP4 file — goes in this folder.

MP4 files are gitignored by default; commit a link to where the video is
hosted (e.g. cloud storage) instead, or remove the `presentation/*.mp4`
ignore rule in `.gitignore` if you want to track the video directly.